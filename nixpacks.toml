[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m pip install -r 'Backend files/requirements.txt'"]

[start]
cmd = "cd 'Backend files' && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"